## Module: <code>postprocessing</code>
A module for splitting N = pq into the large l-bit prime factors p and q given d = (p - 1) / 2 + (q - 1) / 2 - 2^(l - 1).

## Import directive
```python
import quaspy.factoring.rsa.postprocessing
```

## Parent module
- [<code>rsa</code>](../README.md)

## Functions
- [<code>split_N_given_d(d, N)</code>](split_N_given_d.md)

  Splits N = pq into the large l-bit prime factors p and q given d = (p - 1) / 2 + (q - 1) / 2 - 2^(l - 1).

